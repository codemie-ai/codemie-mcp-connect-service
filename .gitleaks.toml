title = "Gitleaks configuration for Codemie MCP Connect Service"

[extend]
useDefault = true

[allowlist]
description = "Allowlisted files"

stopwords = [
  "secret-api-key-123",  # Example API key in example_requests.http
]
